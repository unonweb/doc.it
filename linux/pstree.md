# pstree

```sh
pstree -np # numerisch + PID; zeigt die PID und sortiert sie numerisch
pstree -a # zeigt zuätzlich die Optionen und Argumente an, mit denen ein Prozess gestartet wurde
pstree -G # optisch ansprechendere Ausgabe (manchmal)
```
