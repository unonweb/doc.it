# serve

```sh
npx serve # The quickest way to get started is to run this cmd in your project's directory
npx serve -l 8000 # Specify a URI endpoint on which to listen
npx serve -p # Specify custom port
```